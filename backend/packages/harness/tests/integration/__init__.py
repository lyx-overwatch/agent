"""Integration tests for the SDK end-to-end behavior.

These tests do not require LangChain or any LLM provider; they verify
that the SDK's public surface (PathProvider + Resolver) can be used
with a fresh project that does not adopt DeerFlow conventions.
"""
