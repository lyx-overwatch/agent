// ⚠️ 占位文件（STUB）—— 明天从原项目拷贝真实源码后整体替换本文件。
// 仅保证导出函数签名与 FilePreviewModal、[conversationId]/page.tsx 的调用点对齐。

export function downloadFile(_conversationId: string, _virtualPath: string): void {
  throw new Error('[SkillHub] downloadFile 未接入真实实现（lib/files 占位，待替换）');
}

export function openFileInBrowser(_conversationId: string, _virtualPath: string): void {
  throw new Error('[SkillHub] openFileInBrowser 未接入真实实现（占位，待替换）');
}

export function downloadDirectory(_conversationId: string, _virtualPath: string): void {
  throw new Error('[SkillHub] downloadDirectory 未接入真实实现（占位，待替换）');
}
